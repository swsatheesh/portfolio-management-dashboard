# API Testing

Install VS Code extension:

REST Client (Humao)

Run:

```bash
docker compose -f docker-compose.dev.yml up
```

Open:

```
apps/api/rest/auth.http
```

Click:

- Send Request

Authentication token is automatically extracted after login.